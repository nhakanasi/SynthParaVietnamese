# Copy to .env and fill in whichever backend para_synth/tagging.py is configured to use
# (configs/default.yaml: tagging.backend). Never commit the real .env.

# Qwen (DashScope OpenAI-compatible endpoint) — tagging.backend: qwen
DASHSCOPE_API_KEY=

# Gemini API — tagging.backend: gemini
GEMINI_API_KEY=
